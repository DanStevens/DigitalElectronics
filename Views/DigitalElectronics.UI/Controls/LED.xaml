<UserControl x:Class="DigitalElectronics.UI.Controls.LED"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="20" d:DesignWidth="20">
    <Grid x:Name="_layoutRoot">
        <Grid.Resources>
            <SolidColorBrush x:Key="OnBrush" Color="{Binding OnColor}" />
            <SolidColorBrush x:Key="OffBrush" Color="{Binding OffColor}" />
        </Grid.Resources>
        <Ellipse Stroke="{Binding Stroke}" d:Stroke="Black">
            <Ellipse.Style>
                <Style TargetType="{x:Type Ellipse}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsOn}" Value="true">
                            <Setter Property="Fill" Value="{DynamicResource OnBrush}" />
                        </DataTrigger>
                    </Style.Triggers>
                    <Setter Property="Fill" Value="{DynamicResource OffBrush}" />
                </Style>
            </Ellipse.Style>
        </Ellipse>
        <TextBlock Padding="1" Text="{Binding Text}" TextAlignment="Center" />
    </Grid>
</UserControl>
